# BLUEPRINT.md

AIが人間の宣言（INTENT.md）を読み取り、  
**構造化された設計図（Blueprint）**に変換するためのフレームワーク。

> INTENT.md の Phase 0（宣言）を受け取り、本ファイルの手順で分解・構造化する。

---

## 目的

人間の自然言語による宣言には、以下が暗黙的に含まれている：

- どんな操作単位（スキルモード）が必要か
- どんなデータ構造が必要か
- どんな外部サービスと接続するか
- どんな技術スタックが最適か

**AIの仕事は、これらを宣言から推論し、明示的な設計図に変換すること**だ。

---

## 1. スキルモード抽出

宣言から独立した「モード」（操作単位）を抽出する。

### 抽出ルール

1. **1つの動詞 = 1つのモード候補** — 「評価する」「生成する」「スキャンする」はそれぞれ独立モード
2. **トリガー・入力・出力を明確にする** — 曖昧な場合はユーザーに質問する
3. **宣言のPhilosophyをモードの制約に変換する** — 「キーワードマッチング禁止」→ evaluateモードの制約

### 出力フォーマット

```yaml
skills:
  - name: evaluate
    trigger: "URLが入力されたとき"
    input: "求人URL"
    output: "A-F評価 + スコア + 根拠"
    philosophy: "推論ベース、キーワードマッチング禁止"

  - name: generate-pdf
    trigger: "評価完了後"
    input: "評価結果 + CV"
    output: "ATS最適化PDF"
    constraints: "Space Grotesk + DM Sans, Playwright生成"

  - name: batch
    trigger: "複数URLが入力されたとき"
    input: "URL[]"
    output: "並列評価結果[]"
    constraints: "サブエージェント並列実行"
```

### Career-ops リファレンス

Career-opsの14スキルモードは、このフレームワークで以下のように抽出される：

| # | スキルモード | トリガー | 主な出力 |
|---|------------|---------|---------|
| 1 | evaluate | URL入力 | A-F評価 + スコア |
| 2 | scan | ポータル指定 | 求人リスト |
| 3 | generate-pdf | 評価完了 | ATS最適化PDF |
| 4 | batch | 複数URL入力 | 並列評価結果 |
| 5 | apply | 評価≥4.0 | 応募フォーム入力 |
| 6 | deep-research | 企業指定 | 企業詳細レポート |
| 7 | negotiate | オファー受領 | 交渉スクリプト |
| 8 | linkedin-outreach | ターゲット指定 | アウトリーチメッセージ |
| 9 | interview-prep | 面接日確定 | STAR+Reflectionストーリー |
| 10 | story-bank | 継続蓄積 | マスター回答集 |
| 11 | salary-research | 企業+役職 | 給与レンジレポート |
| 12 | tracker | 全モード | パイプラインエントリー |
| 13 | dashboard | 常時 | Bubble Tea TUIビュー |
| 14 | auto-fill | 応募時 | フォーム自動入力 |

---

## 2. データモデル推論

宣言から必要なエンティティとリレーションを推論する。

### 推論フロー

```
宣言のキーワード → エンティティ候補 → リレーション → スキーマ
```

### 推論ルール

1. **名詞 = エンティティ候補** — 「求人」「評価」「PDF」「ストーリー」はすべてエンティティ候補
2. **動詞 = リレーション候補** — 「評価する」→ User evaluates Job
3. **所有/帰属関係を特定する** — 「自分のCV」→ User has CV
4. **ライフサイクルを考慮する** — 「トラッカー」→ ステータス遷移が必要

### 出力例

```
User ──has──→ CV
User ──evaluates──→ Job
Job ──produces──→ Evaluation
Evaluation ──generates──→ PDF
Evaluation ──feeds──→ Tracker
User ──accumulates──→ StoryBank
```

---

## 3. インテグレーション特定

外部サービスとの接続点を特定する。

### 特定ルール

1. **固有名詞 = 外部サービス** — 「Ashby, Greenhouse, Lever」→ ATS APIインテグレーション
2. **技術的キーワード = ツール依存** — 「PDF生成」→ Playwright / Puppeteer
3. **並列/分散処理 = インフラ依存** — 「並列評価」→ サブエージェント / ワーカーキュー

### 出力フォーマット

| 宣言のキーワード | 外部サービス/ツール | 接続タイプ |
|----------------|-------------------|-----------|
| Ashby, Greenhouse, Lever | ATS API群 | REST API |
| PDF生成 | Playwright | ブラウザ自動化 |
| 並列評価 | Claudeサブエージェント | LLM API |
| ダッシュボード | Bubble Tea | ローカルTUI |
| フォーム自動入力 | Playwright | ブラウザ自動化 |

### リスク分類

| リスクレベル | 条件 | 対応 |
|------------|------|------|
| 低 | 公式API + ドキュメント充実 | そのまま進行 |
| 中 | API制限あり / 非公式API | 代替手段を併記 |
| 高 | スクレイピング / ToS違反の可能性 | ユーザーに明示して判断を求める |

---

## 4. 技術スタック推論

宣言の要件から最適な技術スタックを推論する。  
**人間は技術を指定しなくてよい。** AIが要件から逆算する。

### 推論基準

| 要件カテゴリ | 推論される技術 |
|------------|--------------|
| 並列処理が必要 | Go / Rust / Worker Pool |
| ターミナルUI | Bubble Tea / Ink |
| Web UI | Next.js / Remix |
| セルフホスト | Docker |
| SaaSデプロイ | Vercel / Fly.io |
| 無料枠優先 | Supabase / PlanetScale |
| 自前DB | PostgreSQL |

### 推論フロー

```
宣言の要件
  ↓
パフォーマンス要件を特定
  ↓
UI要件を特定
  ↓
デプロイ要件を特定
  ↓
コスト要件を特定
  ↓
技術スタック候補を生成
  ↓
トレードオフを提示（ユーザーが最終判断）
```

### 注意事項

- AIは技術スタックを**推薦**するが、**決定**はしない
- 複数の選択肢がある場合は、トレードオフを明示してユーザーに選ばせる
- 宣言に明示的な技術指定がある場合は、それを優先する

---

## Devinでの運用ルール

### このファイルの使い方

1. **INTENT.mdの宣言を受け取ったら**、本ファイルの手順に従って分解する
2. **4つの抽出結果（スキルモード・データモデル・インテグレーション・技術スタック）をユーザーに提示する**
3. **ユーザーの承認を得てから、次のPhase（EVALUATE等）に進む**

### 品質チェック

分解結果を提示する前に、以下を確認する：

| チェック項目 | 不足時のアクション |
|------------|-----------------|
| スキルモードが1つもない | 宣言の動詞を再分析する |
| データモデルが宣言と矛盾する | リレーションを見直す |
| インテグレーションのリスクが未評価 | リスク分類を実施する |
| 技術スタックの根拠が不明確 | 推論基準に立ち返る |

### 他のwell-harnessファイルとの連携

| ファイル | BLUEPRINT.mdとの関係 |
|---------|---------------------|
| `INTENT.md` | 入力元。Phase 0の宣言を受け取る |
| `CONSTRAINTS.md` | 技術スタック推論時に、実装制約を参照する |
| `INGEST.md` | 宣言に添付ファイルがある場合、変換ルールを参照する |

---

## アンチパターン

### やってはいけないこと

1. **宣言を読まずに技術選定から始める** — 常にIntentが先、技術は後
2. **全モードを一度に詳細設計する** — コアモードから段階的に
3. **Philosophyを制約に変換し忘れる** — 「4.0未満は推奨しない」はevaluateモードの制約
4. **外部サービスのリスクを評価せずに進む** — ToS違反やレート制限を事前に確認する
5. **人間の暗黙知を無視する** — 宣言に書かれていない前提は質問して明確にする
